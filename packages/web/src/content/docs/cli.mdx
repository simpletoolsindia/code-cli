---
title: CLI
description: BeastCLI CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The BeastCLI CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
beastcli
```

But it also accepts commands as documented on this page. This allows you to interact with BeastCLI programmatically.

```bash
beast run "Explain how closures work in JavaScript"
```

---

### tui

Start the BeastCLI terminal user interface.

```bash
beastcli [project]
```

#### Flags

| Flag         | Short | Description                                                             |
| ------------ | ----- | ----------------------------------------------------------------------- |
| `--continue` | `-c`  | Continue the last session                                               |
| `--session`  | `-s`  | Session ID to continue                                                  |
| `--fork`     |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--prompt`   |       | Prompt to use                                                           |
| `--model`    | `-m`  | Model to use in the form of provider/model                              |
| `--agent`    |       | Agent to use                                                            |
| `--port`     |       | Port to listen on                                                       |
| `--hostname` |       | Hostname to listen on                                                   |

---

## Commands

The BeastCLI CLI also has the following commands.

---

### agent

Manage agents for BeastCLI.

```bash
beast agent [command]
```

---

### attach

Attach a terminal to an already running BeastCLI backend server started via `serve` or `web` commands.

```bash
beast attach [url]
```

This allows using the TUI with a remote BeastCLI backend. For example:

```bash
# Start the backend server for web/mobile access
beast web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
beast attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
beast agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag            | Description                                                                                                                                                                                                                |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--path`        | Directory to write the agent file to (defaults to global or `.beastcli/agent` based on the prompt)                                                                                                                         |
| `--description` | What the agent should do                                                                                                                                                                                                   |
| `--mode`        | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| `--permissions` | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| `--model`, `-m` | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
beast agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
beast auth [command]
```

---

#### login

BeastCLI is powered by the provider list at [Models.dev](https://models.dev), so you can use `beast auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/beastcli/auth.json`.

```bash
beast auth login
```

When BeastCLI starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
beast auth list
```

Or the short version.

```bash
beast auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
beast auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
beast github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
beast github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
beast github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
beast mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
beast mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
beast mcp list
```

Or use the short version.

```bash
beast mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
beast mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
beast mcp auth list
```

Or use the short version.

```bash
beast mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
beast mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
beast mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
beast models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
beast models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in BeastCLI.

```bash
beast models --refresh
```

---

### run

Run beastcli in non-interactive mode by passing a prompt directly.

```bash
beast run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "beast run"
beast run Explain the use of context in Go
```

You can also attach to a running `beast serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
beast serve

# In another terminal, run commands that attach to it
beast run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                             | Short | Description                                                             |
| -------------------------------- | ----- | ----------------------------------------------------------------------- |
| `--command`                      |       | The command to run, use message for args                                |
| `--continue`                     | `-c`  | Continue the last session                                               |
| `--session`                      | `-s`  | Session ID to continue                                                  |
| `--fork`                         |       | Fork the session when continuing (use with `--continue` or `--session`) |
| `--share`                        |       | Share the session                                                       |
| `--model`                        | `-m`  | Model to use in the form of provider/model                              |
| `--agent`                        |       | Agent to use                                                            |
| `--file`                         | `-f`  | File(s) to attach to message                                            |
| `--format`                       |       | Format: default (formatted) or json (raw JSON events)                   |
| `--title`                        |       | Title for the session (uses truncated prompt if no value provided)      |
| `--attach`                       |       | Attach to a running beastcli server (e.g., http://localhost:4096)       |
| `--port`                         |       | Port for the local server (defaults to random port)                     |
| `--dangerously-skip-permissions` |       | Auto-approve permissions that are not explicitly denied (dangerous!)    |

---

### serve

Start a headless BeastCLI server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
beast serve
```

This starts an HTTP server that provides API access to beastcli functionality without the TUI interface. Set `BEAST_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `beast`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage BeastCLI sessions.

```bash
beast session [command]
```

---

#### list

List all BeastCLI sessions.

```bash
beast session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your BeastCLI sessions.

```bash
beast stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
beast export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or BeastCLI share URL.

```bash
beast import <file>
```

You can import from a local file or an BeastCLI share URL.

```bash
beast import session.json
beast import https://opncd.ai/s/abc123
```

---

### web

Start a headless BeastCLI server with a web interface.

```bash
beast web
```

This starts an HTTP server and opens a web browser to access BeastCLI through a web interface. Set `BEAST_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `beast`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
beast acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall BeastCLI and remove all related files.

```bash
beast uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates beastcli to the latest version or a specific version.

```bash
beast upgrade [target]
```

To upgrade to the latest version.

```bash
beast upgrade
```

To upgrade to a specific version.

```bash
beast upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The beastcli CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

BeastCLI can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `BEAST_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `BEAST_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `BEAST_CONFIG`                     | string  | Path to config file                               |
| `BEAST_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `BEAST_CONFIG_DIR`                 | string  | Path to config directory                          |
| `BEAST_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `BEAST_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `BEAST_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `BEAST_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `BEAST_PERMISSION`                 | string  | Inlined json permissions config                   |
| `BEAST_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `BEAST_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `BEAST_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `BEAST_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `BEAST_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `BEAST_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `BEAST_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `BEAST_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `BEAST_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `BEAST_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `BEAST_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `BEAST_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `BEAST_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `BEAST_SERVER_USERNAME`            | string  | Override basic auth username (default `beast`) |
| `BEAST_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `BEAST_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `BEAST_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `BEAST_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `BEAST_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `BEAST_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `BEAST_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `BEAST_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `BEAST_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `BEAST_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `BEAST_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `BEAST_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `BEAST_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `BEAST_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
